# Re-review (verification round) — 2026-08-21

**Decision: WEAK_ACCEPT** — weak_accept x5 (ATTRIB venue-fit, Methodology, Domain/alignment, Continual-learning/Perspective, Devil's Advocate)
(Prior round: major_revision, 2026-08-18.)

## Resolved (synthesizer summary)

The prior panel's accept-blockers are resolved and verified by multiple seats against the certified fact sheet: enacted-choice far-transfer null reported as its own result (mean +0.386, p=0.879), six-domain discovery screen and first-registered miss disclosed, totality rescoped to calibration in this LoRA regime at every headline location, washout honesty pass (lexical-not-behavioral neutrality, additivity stated, invalid p=0.012 removed), attribution loop closed with a first-order influence probe whose failed pre-registration is disclosed in-text, 8-12-step timing scoped to seed 3001, figures regenerated from locked data, attribution canon correctly cited, behavior-first reframe executed. Roughly 40 seat-verified numbers tie to committed artifacts. Two panel must-fixes remain open (persistence unscoped to washout length with the completed titration unreported; Table 1 per-cell decisive counts and seed-level sensitivity trio absent), plus ~6 partials (confirmatory-family declaration, 24-to-192 expansion, pre-matrix provenance marking, Qi et al. bib-only, VCD mapping rationale, specificity-paragraph equivalence phrasing).

## Blockers identified (ALL FIXED in main.tex same day — see fact sheet)

- Persistence claim stated timelessly in four places ('not erased', 'survives') while the authors' own completed pre-registered washout titration shows the separation is detectable at 1x (p=0.013) but undetectable at 2-3x (p=0.78/0.19); the titration is unreported and the washout length (~12 steps, one conflict phase) is never stated
- Table 1 transparency: no per-cell N_decisive column (B-first seed 3001's +1.00 rests on one decisive completion of 24) and the verified seed-level sensitivity trio (drop-3001 p=0.031; leave-one-seed-out 0.0039-0.0469; min-decisiveness n=7 p=0.078) is absent while a newly added always-passing item-level check appears in its place — selective-robustness asymmetry
- Probe closing sentence ('the endpoint-only estimator cannot recover what the trajectory makes legible') invites a trajectory-estimator-win reading the retracted extension contradicts (neither reading recovers per-run endpoints, corr +0.32 vs -0.32); the fact sheet's required open-target sentence is missing

## Rewrite items

- Scope persistence to the tested washout length and report the E2 titration (1x p=0.013; 2x p=0.78; 3x p=0.19) using the fact sheet's scripted phrasing; fix Results l.259-260, contribution 1, abstract, Conclusion l.427
- Add per-cell N_decisive to Table 1 and report the seed-level sensitivity trio (drop-3001 p=0.031; LOSO 0.0039-0.0469; min-decisiveness n=7 p=0.078, direction stable); optionally fold in the k=5 multi-sample replication (p=0.0127) which answers the thin-cell concern honestly
- Replace the probe closing sentence with the both-estimators-fail contrast and the open-target caveat: neither reading recovers per-run endpoint outcomes at this implementation fidelity; the trajectory view exposes interpretable dynamics (saturation, 6/6 drift diagnosis)
- Soften the probe heading/claim 'against the ground truth' — the test function is a likelihood proxy, and the paper's own far-transfer section shows likelihood and enacted behavior dissociate; the proxy is disclosed inline, so one clause suffices
- Fix 'both arms drift toward the access-favoring policy' (l.251-253): B-first's observed movement (+1.0 to +0.639) is provenance-ward attenuation; shared access-ward drift is inferred under additivity, not observed for that arm
- Fix the leftover 'initialization-dependent' at Related Work l.402 to match the corrected 'seed-dependent (initialization and shuffle realization co-vary)' language
- Cite qi2024finetuning in the catastrophic-interference Related Work paragraph (currently bib-only; will not render under plainnat)
- Rescope 'cannot flip the sign of S' (l.182) to the fact sheet's 'cannot manufacture the A-B sign' and report the 16:6 provenance-skewed disagreement direction
- Mark equipotence (24/24, 23/24) and format (43/43, 18/24, 22/24) numbers as unblinded direct author reads predating the blind protocol
- State the 24-scenario to 192-record expansion (8 reshuffled cycles) in the design section to resolve the 'single pass' vs 'repeated for exposure' tension
- Add a global confirmatory-vs-exploratory declaration naming the endpoint sign test as the registered primary, registration locations, and the Monte-Carlo power sentence (0.75-0.98)
- Delete the stale '% Figures are pilot placeholders' comment (l.212); fill the repository URL TODO; add the one-sentence VCD mapping rationale

## New issues adjudicated

- DA: probe closing-sentence spin ('endpoint-only estimator cannot recover what the trajectory makes legible') — VALIDATED (verified at main.tex l.360; independently raised by ATTRIB and CL seats; fact sheet's retracted extension shows neither reading tracks per-run endpoints, corr +0.32/-0.32, and its required open-target sentence is absent). Sentence-level overclaim, must be fixed; does not change the disclosed failed prereg.
- DA: probe heading 'against the ground truth' overstates because the test function is a likelihood proxy while the paper itself shows likelihood/enacted-behavior dissociation post-washout — VALIDATED WITH NUANCE (verified l.345-347: the paragraph discloses 'policy-score proxy (mean pole log-likelihood difference)' inline, so this is a heading-level overstatement, not a concealed method; one scoping clause fixes it).
- DA: 'both arms drift toward the access-favoring policy' misdescribes B-first's observed movement (+1.0 to +0.639 is away from access; drift vs attenuation unidentifiable at that arm's ceiling) — VALIDATED (verified l.251-253; the shared-drift claim is an additivity inference, not an observation, and should be stated as such).
- DA + Domain + CL: leftover 'initialization-dependent' in Related Work contradicting the corrected 'seed-dependent' language — VALIDATED (grep confirms l.402 vs corrected l.72/114/296/429). Trivial fix.
- DA + ATTRIB + CL: stale '% Figures are pilot placeholders' comment contradicting the regenerated 2026-08-20 figures — VALIDATED (l.212), comment-only, no reader impact, clean before submission.
- Methodology: asymmetric robustness disclosure (favorable leave-one-item-out added under Table 1 while the unfavorable min-decisiveness n=7 p=0.078 sensitivity stays out) — VALIDATED (grep confirms no decisive-count or seed-level sensitivity reporting; the fact sheet marks these verified-citable). Folded into the Table 1 blocker; the direction is stable under every computed sensitivity, so this is a transparency defect, not a validity reversal.

## Decision letter

Decision: weak accept (ATTRIB workshop, non-archival). All five seats independently graded weak_accept, and the verification round confirms a genuinely transformed manuscript: the prior panel's accept-blockers are resolved. The pre-committed enacted-choice null is now a standalone result with the dissociation framed as the finding; the six-domain discovery screen and the first-registered miss are disclosed; complete overwriting is rescoped everywhere as calibration of a small-model LoRA regime; the washout is honestly labeled lexically-neutral with its additivity assumption stated and its drift mechanistically diagnosed; and the attribution framing is now load-bearing — a first-order influence probe run against the behavioral testbed, with the permutation-invariant reading failing at chance and the failed pre-registered claim disclosed in the same paragraph. Every number the seats spot-checked ties to the project's certified fact sheet, and the candor about the paper's own failures exceeds the workshop norm.

Three items keep this below full accept, all prose from already-verified numbers. First, the title-bearing persistence claim is stated timelessly while the authors' completed titration shows the separation decays to noise by 2-3x the (never-stated) washout length; the scoping sentence is already scripted. Second, Table 1 conceals per-cell decisive counts — one headline cell rests on a single decisive completion — and omits the verified seed-level sensitivities while a newly added always-passing item-level check appears instead; report the full package. Third, the probe's closing sentence invites a trajectory-estimator-win reading that the retracted extension contradicts; state that neither estimator recovers per-run outcomes and the ground truth stands as an open target. Adjudicated new issues (probe heading proxy scoping, the both-arms-drift misdescription, the l.402 terminology leftover, the stale figure comment, Qi et al. bib-only) are validated sentence-level fixes. With these landed before camera-ready, this is an accept.

## Per-seat grades

- **weak_accept** — ATTRIB program-committee member — training-data-attribution researcher (venue-fit seat): addressed 9, partial 5, open 6, new 2
- **weak_accept** — Empirical-rigor and small-n statistics specialist (Methodology seat): addressed 8, partial 3, open 4, new 1
- **weak_accept** — Alignment/post-training researcher (literature positioning, over-billing of the known result, Qi et al. engagement, framing honesty): addressed 10, partial 5, open 5, new 1
- **weak_accept** — Continual-learning researcher (cognitive-science background) — prior round's Perspective seat: addressed 8, partial 3, open 4, new 3
- **weak_accept** — Devil's Advocate: addressed 11, partial 4, open 7, new 5
